# Self-hosted PostgreSQL Connection
# Replace with your actual database credentials
DATABASE_URL="postgresql://[DB_USER]:[DB_PASSWORD]@[DB_HOST]:5432/[DB_NAME]?schema=public"

# Direct URL for migrations (same as DATABASE_URL for self-hosted)
DIRECT_URL="postgresql://[DB_USER]:[DB_PASSWORD]@[DB_HOST]:5432/[DB_NAME]?schema=public"

# Node environment
NODE_ENV=development

# JWT Secret (generate a strong random string for production)
# Generate with: openssl rand -base64 64
JWT_SECRET="your-super-secret-jwt-key-min-32-chars-change-in-production"

# Site URL
SITE_URL="https://iconiclogs.com"
